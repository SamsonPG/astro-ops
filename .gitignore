node_modules/
dist/
.astro/
.wrangler/
.vercel/
.netlify/
.env
.env.*
!.env.example
*.log
.DS_Store

# NOT ignored on purpose: build-id.js and any file astro-ops emits.
# The whole guarantee is that deploy pipelines ship the COMMITTED id — ignoring it
# recreates the exact stale-cache bug this toolkit exists to prevent.
